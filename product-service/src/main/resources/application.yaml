spring:
  application:
    name: product-service
  data:
    mongodb:
      authentication-database: admin
      username: rootuser
      password: rootpass
      database: product-service
      port: 27017
      host: localhost
      auto-index-creation: true
server:
  port: 3030

